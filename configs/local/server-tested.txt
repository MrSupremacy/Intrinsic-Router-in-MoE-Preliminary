# Core package versions observed on the single-A800 test server (Python 3.12.3).
# Use as pip constraints: pip install -c configs/local/server-tested.txt -e '.[model]'
# The tested torch build is 2.11.0+cu126; choose/install a CUDA build compatible
# with the destination driver first. This is not a full transitive environment lock.
numpy==2.4.3
torch==2.11.0
transformers==4.57.6
datasets==3.6.0
pyarrow==25.0.1
matplotlib==3.11.1
PyYAML==6.0.3
tokenizers==0.22.2
safetensors==0.8.0
sentencepiece==0.2.2
protobuf==6.33.6
